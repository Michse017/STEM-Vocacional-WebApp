############################################################
# STEM Vocational WebApp – Example Environment File
# Copy this to .env and replace placeholder values.
# Never commit the real .env with secrets.
############################################################

# Flask runtime
FLASK_ENV=development
# SECRET_KEY: generate a strong random value in your local .env.
# Example (do NOT use this in production):
SECRET_KEY=change_me_in_local

# Frontend origin allowed for CORS during development
FRONTEND_URL="http://localhost:3000"

# Database (SQL Server) connection settings – provide your own values
DB_SERVER=stemdb.database.windows.net        # Host or host.domain
DB_DATABASE=StemDB                           # Database name
DB_USER=username@server                      # For Azure SQL: user@servername
DB_PASSWORD=                                 # Put the real password ONLY in your local .env
DB_DRIVER={ODBC Driver 17 for SQL Server}    # Installed ODBC driver (17 or 18)
DB_PORT=1433

# Dynamic questionnaires feature: always enabled (no flag required)

# Admin JWT configuration
JWT_EXPIRES_MIN=30          # Access token lifetime (minutes)
JWT_REFRESH_DAYS=7          # Refresh token lifetime (days)
JWT_REFRESH_ROTATE=1        # 1 = rotate refresh tokens, 0 = no rotation

# Optional test runner overrides
# TEST_BASE_URL=http://127.0.0.1:5000

# Optional E2E admin test credentials (only for local testing; never commit real values)
# E2E_ADMIN_CODE=admin_code_here
# E2E_ADMIN_PASSWORD=admin_password_here

# ML model binding: no env vars required by default (artifact path set in metadata)

# IMPORTANT:
# 1. Do NOT commit the real .env file.
# 2. Keep secrets (SECRET_KEY, DB_PASSWORD) out of version control (use CI/CD secrets).
# 3. If adding new env keys, document them in README and RUN.md.
